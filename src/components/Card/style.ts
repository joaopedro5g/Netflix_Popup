import styled from 'styled-components';

import { motion } from 'framer-motion';

export const Container = styled(motion.div)`
  width: 250px;
  height: 100px;
  background: #6532ff;
  border-radius: 10px;
`;

export const TooltipsMore = styled(motion.div)`
  width: 100%;
  height: 290px;
  border-radius: 10px;
  background: #1f1f1f;
`;